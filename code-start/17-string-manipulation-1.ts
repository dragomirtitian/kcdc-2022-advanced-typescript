export {}

type Env = {
    "isAdmin": boolean,
    "isServer": string;
    "maxRequestsPerMinute": number;
}

type EnvData = {
    "getIsAdmin": boolean,
    "getIsServer": string;
    "getMaxRequestsPerMinute": number;
}

const isAdmin = "isAdmin"
const getAdmin = ``;
